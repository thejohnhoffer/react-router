{
  "private": true,
  "scripts": {
    "build:router": "yarn workspace react-router build",
    "build:dom": "yarn workspace react-router-dom build",
    "build:native": "yarn workspace react-router-native build",
    "build": "yarn build:native && yarn build:dom && yarn build:router",
    "clean": "git clean -fdX .",
    "lint": "eslint --cache .",
    "test": "jest"
  },
  "dependencies": {
    "@types/jest": "26.x",
    "@types/jsonfile": "^6.0.1",
    "@types/react": "^17.0.19",
    "@types/react-dom": "^17.0.9",
    "@types/react-native": "*",
    "@types/react-test-renderer": "16.x",
    "@types/semver": "^7.3.8",
    "chalk": "^4.1.1",
    "history": "^5.0.0",
    "jest": "^26.6.3",
    "jsonfile": "^6.1.0",
    "node-fetch": "^2.6.1",
    "prettier": "^2.4.1",
    "prompt-confirm": "^2.0.4",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-test-renderer": "^17.0.2",
    "ts-jest": "^26.5.6"
  },
  "workspaces": {
    "packages": [
      "packages/react-router",
      "packages/react-router-dom",
      "packages/react-router-native"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native/**"
    ]
  },
  "jest": {
    "projects": [
      "<rootDir>/packages/*"
    ]
  }
}
